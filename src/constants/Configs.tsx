import strings from '../i18n';

const Configs = {
  PinCount: 6,
  SuggestEmailList: ['@gmail.com', '@yahoo.com', '@hotmail.com'],
  RegisterStatus: [strings.Incomplete, strings.Completed, strings.Skipped],
};

export default Configs;
